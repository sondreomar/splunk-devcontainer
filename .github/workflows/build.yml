name: Build and push Splunk devcontainer image

on:
  push:
    tags:
      - v*.*.*

concurrency: ${{ github.workflow }}-${{ github.ref }}

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}
  PRIMARY_VARIANT: splunk
  VERSIONS: |
    [
      "9.3.2",
      "9.3.3",
      "9.3.4",
      "9.3.5",
      "9.3.6",
      "9.3.7",
      "9.3.8",
      "9.4.0",
      "9.4.1",
      "9.4.2",
      "9.4.3",
      "9.4.4",
      "9.4.5",
      "9.4.6",
      "9.4.7",
      "10.0.0",
      "10.0.1",
      "10.0.2",
      "10.2.0",
      "10.2.1"
    ]

jobs:
  prepare:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      image_version: ${{ steps.image_semver.outputs.version }}
      image_major: ${{ steps.image_semver.outputs.major }}
      image_minor: ${{ steps.image_semver.outputs.minor }}
      splunk_versions: ${{ steps.splunk_semver.outputs.versions }}
      splunk_latest: ${{ steps.splunk_semver.outputs.latest }}
      splunk_major: ${{ steps.splunk_semver.outputs.major }}
      splunk_minor: ${{ steps.splunk_semver.outputs.minor }}

    steps:
      - name: Determine image version tags
        id: image_semver
        run: |
          # Extract version from tag (v1.0.0 -> 1.0.0)
          version="${GITHUB_REF#refs/tags/v}"

          # Validate semver format (X.Y.Z)
          if ! [[ "$version" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "::error::Invalid semver tag: v$version. Expected format: vX.Y.Z"
            exit 1
          fi

          major=$(echo "$version" | cut -d. -f1)
          minor=$(echo "$version" | cut -d. -f1-2)

          echo "version=$version" >> $GITHUB_OUTPUT
          echo "major=$major" >> $GITHUB_OUTPUT
          echo "minor=$minor" >> $GITHUB_OUTPUT

      - name: Determine splunk version tags
        id: splunk_semver
        run: |
          versions=$(echo "$VERSIONS" | jq -c .)
          sorted=$(echo "$versions" | jq -r '.[]' | sort -V | jq -R . | jq -sc .)

          latest=$(echo "$versions" | jq -r '.[]' | sort -V | tail -1)
          major=$(echo "$sorted" | jq -c 'group_by(split(".")[0]) | map({(last): .[0] | split(".")[0]}) | add')
          minor=$(echo "$sorted" | jq -c 'group_by(split(".")[0:2] | join(".")) | map({(last): .[0] | split(".")[0:2] | join(".")}) | add')

          # Debug output
          echo "All versions:"
          echo "$versions" | jq .
          echo "Latest version: $latest"
          echo "Major version mappings:"
          echo "$major" | jq .
          echo "Minor version mappings:"
          echo "$minor" | jq .

          echo "versions=$versions" >> $GITHUB_OUTPUT
          echo "latest=$latest" >> $GITHUB_OUTPUT
          echo "major=$major" >> $GITHUB_OUTPUT
          echo "minor=$minor" >> $GITHUB_OUTPUT

  build-and-push:
    needs: prepare
    runs-on: ubuntu-latest
    continue-on-error: true
    permissions:
      contents: read
      packages: write
      attestations: write
      id-token: write

    strategy:
      fail-fast: false
      matrix:
        variant: [splunk, universalforwarder]
        version: ${{ fromJson(needs.prepare.outputs.splunk_versions) }}
        include:
          - variant: splunk
            suffix: ""
          - variant: universalforwarder
            suffix: "-uf"
          - version: 9.3.2
            build: "d8bb32809498"
            platform: Linux-x86_64
          - version: 9.3.3
            build: "75595d8f83ef"
            platform: Linux-x86_64
          - version: 9.3.4
            build: "30e72d3fb5f7"
            platform: Linux-x86_64
          - version: 9.3.5
            build: "9848f25c9430"
            platform: Linux-x86_64
          - version: 9.3.6
            build: "8c495c6a1f7d"
            platform: Linux-x86_64
          - version: 9.3.7
            build: "d3040138454b"
            platform: Linux-x86_64
          - version: 9.3.8
            build: "fbf0510dff69"
            platform: Linux-x86_64
          - version: 9.4.0
            build: "6b4ebe426ca6"
            platform: linux-amd64
          - version: 9.4.1
            build: "e3bdab203ac8"
            platform: linux-amd64
          - version: 9.4.2
            build: "e9664af3d956"
            platform: linux-amd64
          - version: 9.4.3
            build: "237ebbd22314"
            platform: linux-amd64
          - version: 9.4.4
            build: "f627d88b766b"
            platform: linux-amd64
          - version: 9.4.5
            build: "8fb2a6c586a5"
            platform: linux-amd64
          - version: 9.4.6
            build: "60284236e579"
            platform: linux-amd64
          - version: 9.4.7
            build: "2a9293b80994"
            platform: linux-amd64
          - version: 10.0.0
            build: "e8eb0c4654f8"
            platform: linux-amd64
          - version: 10.0.1
            build: "c486717c322b"
            platform: linux-amd64
          - version: 10.0.2
            build: "e2d18b4767e9"
            platform: linux-amd64
          - version: 10.2.0
            build: "d749cb17ea65"
            platform: linux-amd64
          - version: 10.2.1
            build: "c892b66d163d"
            platform: linux-amd64

    steps:
      - name: Reclaim ubuntu-latest runner disk space
        run: |
          echo "Disk usage before cleanup:"
          df . -h
          sudo rm -rf \
            /usr/share/swift \
            /usr/share/dotnet \
            /usr/local/lib/android \
            /opt/hostedtoolcache/CodeQL \
            /opt/hostedtoolcache/go
          echo "Disk usage after cleanup:"
          df . -h

      - name: Checkout repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Log in to the Container registry
        uses: docker/login-action@b45d80f862d83dbcd57f89517bcf500b2ab88fb2 # v4.0.0
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@4d04d5d9486b7bd6fa91e7baf45bbb4f8b9deedd # v4.0.0

      - name: Setup Node.js
        uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f # v6.3.0
        with:
          node-version: "20.19.4"

      - name: Setup Dev Container CLI
        run: npm install -g @devcontainers/cli@0.80.0

      - name: Ensure lower case image name
        run: echo "IMAGE_NAME=${IMAGE_NAME,,}" >>${GITHUB_ENV}
        env:
          IMAGE_NAME: ${{ env.IMAGE_NAME }}

      - name: Build devcontainer image
        env:
          VARIANT: ${{ matrix.variant }}
          SUFFIX: ${{ matrix.suffix }}
          SPLUNK_VERSION: ${{ matrix.version }}
          SPLUNK_BUILD: ${{ matrix.build }}
          SPLUNK_PLATFORM: ${{ matrix.platform }}
          IMAGE_REF: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          SPLUNK_LATEST: ${{ matrix.version == needs.prepare.outputs.splunk_latest && 'true' || '' }}
          SPLUNK_MAJOR: ${{ fromJson(needs.prepare.outputs.splunk_major)[matrix.version] || '' }}
          SPLUNK_MINOR: ${{ fromJson(needs.prepare.outputs.splunk_minor)[matrix.version] || '' }}
          IMAGE_VERSION: ${{ needs.prepare.outputs.image_version }}
          IMAGE_MAJOR: ${{ needs.prepare.outputs.image_major }}
          IMAGE_MINOR: ${{ needs.prepare.outputs.image_minor }}
        run: |
          devcontainer build \
            --workspace-folder $GITHUB_WORKSPACE/src/$VARIANT \
            --image-name $IMAGE_REF:$SPLUNK_VERSION$SUFFIX \
            --image-name $IMAGE_REF:$SPLUNK_VERSION$SUFFIX-v$IMAGE_VERSION \
            --image-name $IMAGE_REF:$SPLUNK_VERSION$SUFFIX-v$IMAGE_MINOR \
            --image-name $IMAGE_REF:$SPLUNK_VERSION$SUFFIX-v$IMAGE_MAJOR \
            ${SPLUNK_MINOR:+--image-name $IMAGE_REF:${SPLUNK_MINOR}${SUFFIX}} \
            ${SPLUNK_MINOR:+--image-name $IMAGE_REF:${SPLUNK_MINOR}${SUFFIX}-v${IMAGE_VERSION}} \
            ${SPLUNK_MINOR:+--image-name $IMAGE_REF:${SPLUNK_MINOR}${SUFFIX}-v${IMAGE_MINOR}} \
            ${SPLUNK_MINOR:+--image-name $IMAGE_REF:${SPLUNK_MINOR}${SUFFIX}-v${IMAGE_MAJOR}} \
            ${SPLUNK_MAJOR:+--image-name $IMAGE_REF:${SPLUNK_MAJOR}${SUFFIX}} \
            ${SPLUNK_MAJOR:+--image-name $IMAGE_REF:${SPLUNK_MAJOR}${SUFFIX}-v${IMAGE_VERSION}} \
            ${SPLUNK_MAJOR:+--image-name $IMAGE_REF:${SPLUNK_MAJOR}${SUFFIX}-v${IMAGE_MINOR}} \
            ${SPLUNK_MAJOR:+--image-name $IMAGE_REF:${SPLUNK_MAJOR}${SUFFIX}-v${IMAGE_MAJOR}} \
            ${SPLUNK_LATEST:+--image-name $IMAGE_REF:latest${SUFFIX}}

      - name: Push devcontainer image
        run: |
          docker push --all-tags ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}

  delete-untagged-images:
    needs: build-and-push
    runs-on: ubuntu-latest
    permissions:
      packages: write

    steps:
      - name: Trim repository owner from image name
        run: echo "IMAGE_NAME=$( basename ${{ env.IMAGE_NAME }} )" >>${GITHUB_ENV}

      - name: Delete untagged images
        uses: actions/delete-package-versions@e5bc658cc4c965c472efe991f8beea3981499c55 # v5.0.0
        env:
          PACKAGE_NAME: ${{ env.IMAGE_NAME }}
        with:
          package-type: container
          package-name: ${{ env.PACKAGE_NAME }}
          delete-only-untagged-versions: true
          token: ${{ secrets.GITHUB_TOKEN }}
